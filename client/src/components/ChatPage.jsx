import React from 'react';
import PropTypes from 'prop-types';

const ChatPage = props => {
    return (
        <div>

        </div>
    );
};

ChatPage.propTypes = {

};

export default ChatPage;